import { Animals } from "../../domain/animals";
import { AnimalsRepository } from "../repositories/animals-repository";
/*
export class UpdateAnimalsUseCase {
    constructor(
        private animalsRepository: AnimalsRepository
    ) {}

    // Torne o método assíncrono
    async execute(id: string, bodyParams: Partial<Animals>): Promise<Animals | null> {
        const updatedAnimal = await this.animalsRepository.update(id, bodyParams);
        return updatedAnimal;
    }
}*/
